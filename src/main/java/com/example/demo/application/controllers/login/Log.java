package com.example.demo.application.controllers.login;

import javafx.fxml.FXML;

public class Log {
    @FXML
    public void enter(){
        // Deve verificar se o usuário e a senha estão corretos e abrir a página de dashboard.
        // Não é necessário que tenhamos um sistema para autenticação para o usuário, então sugiro que o usuário padrão seja "admin" e a senha seja "1234".
    }
}
